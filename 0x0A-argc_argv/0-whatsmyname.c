#include "holberton.h"
#include <stdio.h>

/**
 * main - main function.
 * @argc: count the numbers of arguments
 * @argv: argument vector
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
if (argc == 1)
printf("%s\n", argv[0]);
return (0);
}
