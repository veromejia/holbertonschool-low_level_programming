#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void print_alphabet_x10(void);
int main(void)
{
print_alphabet_x10();
return (0);
}

void print_alphabet_x10(void)
{
char i;
int count;
 for (count = 0; count <= 10; count++)
   {
     i= 'a';
     while (i <= 'z')
       {
	 putchar(i);
	 i++;
       }
     putchar('\n');
   }
}
