#include "holberton.h"
#include <stdio.h>

/**
 * factorial - print the factorial of n number
 * @n:given number
 * Return: factorial of a number.
 */
int factorial(int n)
{
  if (n < 0)
    return (-1);
  else if (n == 0)
    return (1);
    else
      return(n * factorial(n-1));
  
}
