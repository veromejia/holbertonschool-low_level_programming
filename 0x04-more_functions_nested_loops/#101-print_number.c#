#include "holberton.h"
#include <stdio.h>
/**
 * print_number - print an integer with _putchar
 * @n: number
 * Return: always 0
 */
void print_number(int n)
{
  if (n

  if (n / 10)
print_number(n / 10);
_putchar(n % 10 + '0');
}
